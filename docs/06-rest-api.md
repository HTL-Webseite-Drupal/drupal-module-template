# REST API

This module includes a small REST example using Drupal Core REST.

Features:
- GET endpoint
- JSON output
- Uses the same service as UI code

This is intentionally overkill for demo purposes,
but shows the correct Drupal way.

## Example

The example demonstrates how to:
- Create a REST resource
- Use a service in the resource
- Return JSON data
- Test the endpoint using curl
